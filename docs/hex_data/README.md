# Hex static datasets

Generated by `setup/06_extract_hex_static.py` on **2026-06-03** from `analytics.marts.mart_provider_outliers` (7,062,726 rows).

These small CSVs back the published Hex app's interactive cells so the MAD-threshold slider, metric / specialty / state filters, and the drill-down keep working **after the Snowflake trial warehouse lapses**. See `docs/hex_notebook_spec.md` → "Static / offline mode" for the rewired Hex cells and the fidelity notes.

| File | Rows | Backs |
|---|---:|---|
| `tab1_overview_kpis.csv` | 1 | Tab 1 KPI tiles |
| `tab2_outliers_top1000.csv` | 6,000 | Tab 2 outlier table (top-1000 per metric, peer floor 30 applied) |
| `tab3_demo_providers.csv` | 6 | Tab 3 drill-down summary (curated demo NPIs) |
| `tab3_demo_peer_dist.csv` | 6,978 | Tab 3 peer-distribution histogram (curated demo NPIs) |
| `tab4_state_aggregates.csv` | 59 | Tab 4 state map |

Refresh (while the warehouse is live):

```bash
.venv/bin/python setup/06_extract_hex_static.py
```
